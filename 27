/*
27. Em Matemática, o número harmônico designado por H(n) define-se como sendo a soma da série harmônica:

       H(n) = 1 + 1/2 + 1/3 + 1/4 + ... + 1/n

Faça um programa que leia um valor n inteiro e positivo e apresente o valor de H(n).



Passo a passo com um exemplo (n = 4)

Inicialmente: H = 0

i = 1: H = 0 + 1/1 = 1.0

i = 2: H = 1.0 + 1/2 = 1.5

i = 3: H = 1.5 + 1/3 ≈ 1.8333

i = 4: H = 1.8333 + 1/4 = 2.0833

*/

#include <stdio.h>   // biblioteca padrão de entrada e saída
#include <stdlib.h>  // usada para system("pause")

int main()
{
    int n;           // variável para armazenar o número inteiro positivo
    float H = 0;    //  acumuladora. variável para acumular a soma harmônica


    // solicita ao usuário que digite um número inteiro positivo
    printf("Digite um numero inteiro positivo: ");
    scanf("%d", &n);

    // verifica se o número digitado é positivo
    if(n <= 0)
    {
        printf("Por favor, digite um numero maior que zero.\n");
        system("pause");
        return 0; // encerra o programa se o número não for positivo
                  // obs: para não ser preciso encerrar o programa com return 0, poderia colocar na sequência o else, isso é só um jeito diferente de fazer o código
    }

    // loop para calcular a soma harmônica
    for(int i = 1; i <= n; i++)
    {
        H = H + (1.0 / i); // soma 1 dividido pelo valor atual de i
    }

    // exibe o resultado com 4 casas decimais
    printf("O valor de H(%d) eh: %.4f\n", n, H);

    system("pause"); // pausa o programa antes de encerrar (útil no Windows)
    return 0; // indica que o programa terminou corretamente
}
