/*
28. Faça um programa que leia um valor N inteiro e positivo, calcule e mostre o valor E,
conforme a fórmula a seguir:

          E = 1 + 1/1! + 1/2! + 1/3! + ... + 1/N!
*/

#include <stdio.h>   // biblioteca padrão de entrada e saída
#include <stdlib.h>  // usada para system("pause")

int main()
{
    int N;           // variável para armazenar o número informado pelo usuário
    float E = 1.0;  // variável para armazenar a soma (começa com 1, pois o primeiro termo é 1)
    float fatorial = 1.0; // variável auxiliar para calcular os fatoriais

    // pede para o usuário digitar um número inteiro positivo
    printf("Digite um numero inteiro positivo: ");
    scanf("%d", &N);

    // verifica se o número é válido
    if(N < 0)
    {
        printf("Por favor, digite um numero positivo.\n");
        system("pause");
        return 0; // encerra o programa se o número for inválido
    }

    // laço que calcula o somatório da expressão
    for(int i = 1; i <= N; i++)
    {
        fatorial = fatorial * i; // calcula o fatorial atual (ex: 1!, 2!, 3!, ...)
        E = E + (1.0 / fatorial); // soma 1 dividido pelo fatorial atual
    }

    // exibe o resultado final com 6 casas decimais
    printf("O valor de E para N = %d eh: %.6f\n", N, E);

    system("pause"); // pausa o programa no Windows
    return 0; // indica que o programa terminou corretamente
}

