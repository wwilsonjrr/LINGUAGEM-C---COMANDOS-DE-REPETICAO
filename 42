/*
42. Faça um programa que leia um conjunto não determinado de valores, um de cada vez,
e escreva para cada um dos valores lidos: o quadrado, o cubo e a raiz quadrada.
Finalize a entrada de dados com um valor negativo ou zero.
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída (printf, scanf)
#include <stdlib.h>  // Biblioteca padrão (para system("pause"))
#include <math.h>    // Biblioteca matemática (para pow e sqrt)

int main()
{

    float numero;  // Variável para armazenar o número digitado pelo usuário

    // Mensagem inicial
    printf("=============================================\n");
    printf("   CALCULO DE QUADRADO, CUBO E RAIZ QUADRADA\n");
    printf("=============================================\n\n");

    // Laço infinito — o programa só termina quando o usuário digitar 0 ou número negativo
    while (1)
    {

        // Solicita um número ao usuário
        printf("Digite um numero (negativo ou zero para encerrar): ");
        scanf("%f", &numero);

        // Condição de parada: encerra se o número for menor ou igual a zero
        if (numero <= 0)
        {
            printf("\nEncerrando o programa...\n");
            break;  // Sai do laço while
        }

        // Calcula o quadrado, o cubo e a raiz quadrada do número
        float quadrado = pow(numero, 2);  // pow(base, expoente)
        float cubo = pow(numero, 3);
        float raiz = sqrt(numero);        // sqrt() calcula a raiz quadrada

        // Exibe os resultados formatados
        printf("Numero: %.2f\n", numero);
        printf("Quadrado: %.2f\n", quadrado);
        printf("Cubo: %.2f\n", cubo);
        printf("Raiz quadrada: %.2f\n\n", raiz);
    }

    system("pause");  // Mantém o console aberto até o usuário pressionar uma tecla (Windows)
    return 0;         // Indica que o programa terminou corretamente
}

